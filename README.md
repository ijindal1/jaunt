# jaunt

Small Python library with a CLI, managed with `uv`.

## Quickstart

```bash
uv sync
uv run jaunt
uv run jaunt hello Ishita
```

## Dev

```bash
uv run ruff check .
uv run ty check
uv run pytest
```
